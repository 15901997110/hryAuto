<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.haier.mapper.QueryAllVoMapper">
 <!--暂未完成-->
  <select id="queryAllVoListByCondition" parameterType="QueryAllVo" resultType="QueryAllVo">
     SELECT * FROM tcase LEFT JOIN ti ON tcase.`iId`=ti.`id`
     LEFT JOIN tservice ON ti.`serviceId`=tservice.`id`
      LEFT JOIN tenvdetail ON tservice.`id`=tenvDetail.`serviceId`
      WHERE

  </select>

</mapper>